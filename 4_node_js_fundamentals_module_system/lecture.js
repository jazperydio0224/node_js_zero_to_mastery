/* require() function
// not part of vanilla javascript
// takes a javascript file, executes it, and returns the code from that file so that we can reuse it elsewhere
* examples: require('http'), require('events'), const EventEmitter = require('events');
*/

/* MAKING HTTPS REQUESTS

*** ILLUSTRATION on 'http-example.js'
*/

/* WHY USE MODULES?
1. Reuse existing code.
2. Organize your code.
3. Expose only what will be used.
*/
